name: 'Pull requests pre-checks'
on:
  - pull_request
jobs:
  pre-checks:
    name: 'pre-checks'
    runs-on: ubuntu-latest
    steps:
      - name: 'Checkout'
        uses: actions/checkout@master
      
      - name: tfsec
        uses: aquasecurity/tfsec-action@v1.0.0




